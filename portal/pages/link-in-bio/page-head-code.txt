<!-- FATHERS FRONT - LINK IN BIO PAGE HEAD -->
<style>
@font-face {
  font-family: "FF Barlow Condensed";
  src: url("https://fathersfront.com/fonts/site/barlow-condensed-700.woff2") format("woff2");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "FF Source Serif 4";
  src: url("https://fathersfront.com/fonts/site/source-serif-4-600-italic.woff2") format("woff2");
  font-weight: 600;
  font-style: italic;
  font-display: swap;
}

.ff-bio,
.ff-bio * {
  box-sizing: border-box;
}

.ff-bio {
  --ink: #171412;
  --cream: #f4ecdc;
  --oxblood: #912d2f;
  --ember: #e84509;
  min-height: 100svh;
  margin: 0;
  padding: 42px 18px 30px;
  color: var(--ink);
  background: var(--cream);
  font-family: Arial, Helvetica, sans-serif;
}

.ff-bio__shell {
  width: min(100%, 620px);
  margin: 0 auto;
}

.ff-bio__brand {
  display:flex;
  justify-content:center;
  gap:.12em;
  color:var(--ink);
  font:700 clamp(42px,10vw,64px)/.8 "FF Barlow Condensed",Impact,sans-serif;
  letter-spacing:-.035em;
  text-transform:uppercase;
}

.ff-bio__brand span:last-child {
  color:var(--oxblood);
}

.ff-bio__rule {
  width:72px;
  height:2px;
  margin:30px auto;
  background:var(--oxblood);
}

.ff-bio__intro {
  max-width:520px;
  margin:0 auto 32px;
  color:#665e55;
  font-size:15px;
  line-height:1.65;
  text-align:center;
}

.ff-bio__intro strong {
  display:block;
  margin-bottom:8px;
  color:var(--ink);
  font:700 22px/1.05 "FF Barlow Condensed",Impact,sans-serif;
  letter-spacing:.04em;
  text-transform:uppercase;
}

.ff-bio__intro em {
  color:var(--ember);
  font:600 italic 20px/1.2 "FF Source Serif 4",Georgia,serif;
}

.ff-bio__links {
  display:grid;
  gap:12px;
}

.ff-bio__link {
  display:grid;
  grid-template-columns:minmax(0,1fr);
  gap:16px;
  align-items:center;
  min-height:68px;
  padding:14px 18px;
  color:var(--ink);
  background:transparent;
  border:1px solid #9f9487;
  text-decoration:none;
  transition:background 160ms ease,border-color 160ms ease,color 160ms ease;
}

.ff-bio__link:hover,
.ff-bio__link:focus-visible {
  color:var(--cream);
  background:var(--oxblood);
  border-color:var(--oxblood);
}

.ff-bio__link strong {
  display:block;
  font:700 20px/1 "FF Barlow Condensed",Impact,sans-serif;
  letter-spacing:.04em;
  text-transform:uppercase;
}

.ff-bio__link small {
  display:block;
  margin-top:5px;
  color:#665e55;
  font-size:11px;
  line-height:1.35;
}

.ff-bio__link:hover small,
.ff-bio__link:focus-visible small {
  color:var(--cream);
}

.ff-bio__link--primary {
  border:2px solid var(--oxblood);
}

.ff-bio__book-link {
  grid-template-columns:48px minmax(0,1fr);
}

.ff-bio__book-cover {
  display:block;
  width:38px;
  height:52px;
  object-fit:cover;
  box-shadow:5px 6px 0 rgba(23,20,18,.18);
}

.ff-bio__social {
  display:flex;
  justify-content:center;
  gap:10px;
  margin:26px 0 0;
}

.ff-bio__social a {
  display:grid;
  width:42px;
  height:42px;
  place-items:center;
  color:var(--ink);
  border:1px solid #9f9487;
  text-decoration:none;
  transition:color 160ms ease,background 160ms ease,border-color 160ms ease;
}

.ff-bio__social a:hover,
.ff-bio__social a:focus-visible {
  color:var(--cream);
  background:var(--oxblood);
  border-color:var(--oxblood);
}

.ff-bio__social svg {
  width:20px;
  height:20px;
  fill:none;
  stroke:currentColor;
  stroke-width:1.8;
}

.ff-bio__social-fill {
  fill:currentColor;
  stroke:none;
}

.ff-bio__footer {
  margin:30px 0 0;
  color:#756c63;
  font-size:11px;
  letter-spacing:.12em;
  text-align:center;
  text-transform:uppercase;
}

@media (max-width: 480px) {
  .ff-bio {
    padding-top:32px;
  }

  .ff-bio__link {
    min-height:64px;
  }
}
</style>
